/**
 * Created by yyj on 19/12/2016.
 */
import { connect } from 'react-redux';
import SharePage from '../components/RecipeBox';

export default connect(
  (state) => ({
  }),
  (dispatch) => ({
  })
)(SharePage);
